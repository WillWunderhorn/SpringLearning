spring:
  main:
    banner-mode: off
  application:
    name: author
  config:
    import: application.yml
  cloud:
    consul:
      config:
        enabled: false
      discovery:
        enabled: false