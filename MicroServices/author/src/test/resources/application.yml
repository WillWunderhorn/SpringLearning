spring:
  main:
    banner-mode: off
  application:
    name: author
  config:
    import: application.yml
  liquibase:
    contexts: test
  cloud:
    consul:
      config:
        enabled: false
      discovery:
        enabled: false